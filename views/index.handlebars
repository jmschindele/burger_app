<h1> Burgers man!</h1>

<h2>Fresh of the grill</h2>

<ul>
    {{#each burgers}}
        {{#unless devoured}}
          {{> burgers/burger-block devoured=true}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Fallen burgers</h2>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            {{> burgers/burger-block devoured=false}}
        {{/if}}
     {{/each}}
</ul>

<h2>Give us your recipe</h2>
<form class="create-form">
    
    <div class="form-group">
        <label for="ba">Burger Name:</label>
        <input type="text" id="ba" name="name">
    </div>
    <div class="form-group">
        <label for='devoured'>Devoured?</label><br>
        <input type='radio' name='devoured' value='0' checked> Devoured <br>
        <input type="radio" name='devoured' value='1'> Not Eaten
    </div>
    <button type="submit">Add Burger</button>
</form>